{
  "name": "@dcos/connection-manager",
  "main": "./dist/index.js",
  "module": "./src/index.js",
  "version": "0.2.1",
  "license": "Apache-2.0",
  "repository": "https://github.com/dcos-labs/connection-manager",
  "scripts": {
    "test": "jest src",
    "build": "babel src --out-dir dist",
    "prepublishOnly": "npm run build"
  },
  "jest": {
    "transform": {
      "^.+\\.js?$": "babel-jest"
    }
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-jest": "^21.2.0",
    "babel-preset-env": "^1.6.1",
    "jest": "22.4.3",
    "jest-cli": "22.4.3"
  },
  "dependencies": {
    "@dcos/connections": "^0.1.0",
    "immutable": "^3.8.2"
  }
}
